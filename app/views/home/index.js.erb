$('#activities').append("<%= j render_activities(@activities, layout: :activity) %>");

<% if @activities.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@activities) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $(".pagination").remove();
<% end %>