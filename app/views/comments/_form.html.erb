<%= react_component 'CommentsForm', { url: comments_path(commentable_type: c.class.to_s, commentable_id: c.id),
  authenticity_token: form_authenticity_token, linkTo: users_path(current_user), avatar: current_user.avatar.url } %>
