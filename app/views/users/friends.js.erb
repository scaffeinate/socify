$('#users').append("<%= j render(@friends) %>");

<% if @friends.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@friends) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $(".pagination").remove();
<% end %>