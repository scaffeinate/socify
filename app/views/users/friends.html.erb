<div class="row">
  <%= render 'shared/sidebar/left' %>
  <div class="stream">
    <div id="friends" class="resources-block">
      <h4 class="underlined">FRIENDS</h4>
      <%= render(@friends) || render('shared/no_resource', resources: 'Friends') %>
    </div>
    <div class="pagination-block">
      <%= render 'shared/paginate', resources: @friends %>
    </div>
  </div>
  <%= render 'shared/sidebar', page: 'friends' %>
</div>
