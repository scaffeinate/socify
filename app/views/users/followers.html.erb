<div class="row">
  <%= render 'shared/user_board' %>
  <div class="col-md-6 well">
    <h3>Followers</h3>
    <hr>
    <div id="followers">
      <%= render(@followers) || render('shared/no_resource', resources: 'Followers')  %>
    </div>
    <%= render 'shared/paginate', resources: @followers %>
  </div>
  <%= render 'shared/sidebar', page: 'followers' %>
</div>
