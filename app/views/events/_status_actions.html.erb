<% if user_signed_in? %>
  <%= react_component 'EventStatusActions', { eventId: event.id, status: current_user.attending_event_status(event.id), authenticityToken: form_authenticity_token } %>
<% end %>
