<%= react_component 'EventsForm', {
    authenticityToken: form_authenticity_token,
    event: @event.persisted? ? @event.to_json : nil,
    attachment: @event.attachment ? @event.attachment.file_name.cover.url : nil
  }
%>
