<% if activity.trackable %>
  <%= render 'partials/avatar', user: activity.trackable.follower %> <%= activity.trackable.follower.name %> follows <%= render 'partials/avatar', user: activity.trackable.followable %> <%= activity.trackable.followable.name %><hr>
<% end %>