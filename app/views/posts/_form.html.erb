<%= react_component 'PostsForm', {
    authenticity_token: form_authenticity_token,
    postId: @post.id,
    attachment: @post.attachment.url,
    postContent: @post.content,
    linkPreviewHTML: @post.preview_html
  }
%>
