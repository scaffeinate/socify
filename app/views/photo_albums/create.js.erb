<% if @photo_album.persisted? %>
  $("#photo-dropzone").fadeIn();
  $("#photo_album_id").val("<%= @photo_album.id %>");
  alertify.success("Album created");
<% else %>
  alertify.error("<%= @photo_album.errors.full_messages.first %>");
<% end %>
